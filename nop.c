#include "monty.h"
/**
 * nop - do nothing
 * @stack: pointer to a pointer to the stack
 * @line_number: line number
 * Return: void
 */
void nop(stack_t **stack, unsigned int line_number)
{
	/* avoid unused varaiable warning */
	(void)stack;
	(void)line_number;
}
